# 任务计划

## 目标
根据当前前端界面实现，核对以下两份文档的业务逻辑是否一致、是否缺失、是否存在冲突，并整理需要用户确认的问题。

- `水务系统生产测试模块_业务设计文档v1.0 (1).md`
- `最终prd_水务系统第一阶段_生产测试模块.md`

## 阶段
| 阶段 | 状态 | 内容 |
| --- | --- | --- |
| 1 | completed | 读取前端结构与两份文档，建立页面-流程-字段映射 |
| 2 | completed | 对照前端逐页核对两份文档的流程、状态、操作、字段和约束 |
| 3 | completed | 输出差异清单、风险点和待确认问题 |

## 决策
- 以前端现状为基准核对文档，不默认文档一定正确。
- 若代码和两份文档都无法唯一确定逻辑，则记录为待确认问题并向用户提问。
- 用户已确认：包装管理固定 `10` 只/箱；批量测试“保存历史”必须真实保存到历史表。

## 错误记录
| 错误 | 尝试 | 处理 |
| --- | --- | --- |
| `rg` 扫描 `Warehouse/Archive/Announcement/Permissions` 关键词超时 | 1 | 改为按文件分段读取和小范围关键词检索，避免一次性输出过大 |
